# PharmaSentinel — Streamlit Cloud requirements
# Heavy ML deps (torch, transformers) are excluded from the demo deployment.
# The app runs in demo/mock mode without a trained checkpoint.

numpy>=1.26.0
pandas>=2.1.0
plotly>=5.18.0
streamlit>=1.32.0
streamlit-option-menu>=0.3.12
scikit-learn>=1.4.0
scipy>=1.12.0
